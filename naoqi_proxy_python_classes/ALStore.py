#!/usr/bin/env python
# Class autogenerated from /home/sam/Downloads/softbank/naoqi-sdk-2.5.5.5-linux64/include/alproxies/alstoreproxy.h
# by Sammy Pfeiffer's <Sammy.Pfeiffer at student.uts.edu.au> generator
# You need an ALBroker running

from naoqi import ALProxy



class ALStore(object):
    def __init__(self):
        self.proxy = None

    def force_connect(self):
        self.proxy = ALProxy("ALStore")

    def ping(self):
        """Just a ping. Always returns true

        :returns bool: returns true
        """
        if not self.proxy:
            self.proxy = ALProxy("ALStore")
        return self.proxy.ping()

    def status(self):
        """Get package status.

        :returns std::vector<AL::ALValue>: 
        """
        if not self.proxy:
            self.proxy = ALProxy("ALStore")
        return self.proxy.status()

    def stopUpdate(self):
        """Stop the update.
        """
        if not self.proxy:
            self.proxy = ALProxy("ALStore")
        return self.proxy.stopUpdate()

    def update(self):
        """Update applications
        """
        if not self.proxy:
            self.proxy = ALProxy("ALStore")
        return self.proxy.update()

    def version(self):
        """Returns the version of the module.

        :returns str: A string containing the version of the module.
        """
        if not self.proxy:
            self.proxy = ALProxy("ALStore")
        return self.proxy.version()
